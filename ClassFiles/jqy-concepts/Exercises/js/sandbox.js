/********************************
 * Selecting                    *
 ********************************/
$(document).ready(
	function() {
		
    	//Select all of the div elements that have a class of 'module'. Add the 'showMe' class to them.
		
		//Come up with three selectors that you could use to get the third item in the #myList unordered list. Try each of them using the 'showMe2' class. Which is the best to use? Why?
    
		//Select the label for the search input using an attribute selector. Add the 'showMe2' class to it.
    
		//Figure out how many elements on the page are hidden (hint: .length). Display in the console.
    
		//Figure out how many image elements on the page have an alt attribute. Display in the console.
		
		//Select all of the odd table rows in the table body. Add the 'oddRow' class to them.

		
	}
);